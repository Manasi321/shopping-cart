//import { TestBed } from '@angular/core/testing';
//import { RouterTestingModule } from '@angular/router/testing';
//import { AppComponent } from './app.component';

import { add, constant, subtract } from "cypress/types/lodash";



/*describe('AppComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [
        RouterTestingModule
      ],
      declarations: [
        AppComponent
      ],
    }).compileComponents();
  });

  it('should create the app', () => {
    const fixture = TestBed.createComponent(AppComponent);
    const app = fixture.componentInstance;
    expect(app).toBeTruthy();
  });

  it(`should have as title 'my_shop'`, () => {
    const fixture = TestBed.createComponent(AppComponent);
    const app = fixture.componentInstance;
    expect(app.title).toEqual('my_shop');
  });

  it('should render title', () => {
    const fixture = TestBed.createComponent(AppComponent);
    fixture.detectChanges();
    const compiled = fixture.nativeElement as HTMLElement;
    expect(compiled.querySelector('.content span')?.textContent).toContain('my_shop app is running!');
  });
});*/

/*fdescribe( 'AppComponent', () => {
  it('First Test Script', () => {
    const tax = 10*2;
    expect(tax).toBe(20);
  });
});*/

/*fdescribe('AppComponent', () => {
  it('should add two numbers', () => {
    const add = 2+3;
    expect(add).toBe(5);
  });

  it('should subtract two numbers', () => {
    const subtract = 5-3;
    expect(subtract).toBe(2);
  });
});*/